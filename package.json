{
    "name": "chatterbox-root",
    "version": "1.0.0",
    "description": "Root configuration for Chatterbox deployment",
    "scripts": {
        "install-all": "npm install && npm install --prefix chatterbox-client && npm install --prefix chatterbox-server",
        "build": "npm install --prefix chatterbox-client && npm run build --prefix chatterbox-client && npm install --prefix chatterbox-server",
        "start": "node chatterbox-server/index.js",
        "dev": "concurrently \"npm run dev --prefix chatterbox-server\" \"npm run dev --prefix chatterbox-client\""
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}